write me a powershell script that performs basic system enumeration, displays environment variables, gathers network information including DNS Servers, the ARP Cache and the routing table. List connected drives and firewall configuration, list current user, user privileges, local users, local groups, and local administrators, list User Directories, search for SAM backup files, list running processes, list installed software directories, list software in registry, list folders with Everyone permissions, check registry for AlwaysInstallElevated, check for unquoted service paths, list scheduled tasks, list the tasks folder
